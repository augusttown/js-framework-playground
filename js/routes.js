
define(['app'], function (app) {

    'use strict';

    /*return app.config(function($locationProvider, $stateProvider, $urlRouterProvider) {

        $stateProvider
            .state('home', {
                url: '/home',
                component: 'simpleDimpleChart',
                label: 'Home'
            });

        $urlRouterProvider.otherwise("/home");
    });*/


});